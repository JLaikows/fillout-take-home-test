{
  "name": "fillout-take-home-test",
  "version": "1.0.0",
  "description": "A take home test for fillout, hitting their response endpoint with filtering",
  "main": "dist/index.ts",
  "scripts": {
    "build": "tsc",
    "build-watch": "tsc --watch",
    "start": "node --env-file=.env index.js",
    "dev": "nodemon --env-file=.env dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Joshua Laikowski",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.7",
    "body-parser": "^1.20.2",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.5",
    "@types/express": "^4.17.21",
    "nodemon": "^3.1.0",
    "typescript": "^5.4.2"
  }
}
